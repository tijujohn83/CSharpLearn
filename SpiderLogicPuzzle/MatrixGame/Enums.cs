using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace MatrixGame
{
    public enum PlayerCount
    {
        One = 1,
        Two = 2,
        Three = 3,
        Four = 4
    }

    public enum Player
    {
        Draw = 0,
        PlayerA = 1,
        PlayerB = 2,
        PlayerC = 3,
        PlayerD = 4
    }
}
